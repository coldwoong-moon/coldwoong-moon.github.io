{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{ range .Data.Pages }}
    {{ if not (in .Site.Params.sitemap.excludedKinds .Kind) }}
      {{ if and (.Param "xml" | default true) (not (isset .Params "externalUrl")) }}
        {{- if .Permalink -}}
          <url>
            <loc>{{ .Permalink }}</loc>{{ if not .Lastmod.IsZero }}
            <lastmod>{{ safeHTML ( .Lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>{{ else }}
            <lastmod>{{ safeHTML ( .Date.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>{{ end }}{{ with .Sitemap.ChangeFreq }}
            <changefreq>{{ . }}</changefreq>{{ else }}{{ if eq .Kind "home" }}
            <changefreq>daily</changefreq>{{ else if eq .Kind "section" }}
            <changefreq>weekly</changefreq>{{ else }}
            <changefreq>monthly</changefreq>{{ end }}{{ end }}{{ if ge .Sitemap.Priority 0.0 }}
            <priority>{{ .Sitemap.Priority }}</priority>{{ else }}{{ if eq .Kind "home" }}
            <priority>1.0</priority>{{ else if eq .Kind "section" }}
            <priority>0.8</priority>{{ else if and (eq .Kind "page") (eq .Type "posts") }}
            <priority>0.9</priority>{{ else }}
            <priority>0.7</priority>{{ end }}{{ end }}{{ if .IsTranslated }}{{ range .Translations }}
            <xhtml:link
              rel="alternate"
              hreflang="{{ .Language.Lang }}"
              href="{{ .Permalink }}"
              />{{ end }}
            <xhtml:link
              rel="alternate"
              hreflang="{{ .Language.Lang }}"
              href="{{ .Permalink }}"
              />{{ end }}
          </url>
        {{- end -}}
      {{ end }}
    {{ end }}
  {{ end }}
</urlset>
